# A5 Particle Physics Discovery

## Discovery
We found an exact formula for particle masses:
m = m_e × φ^(q/4)
where q = a×8 + b×15 + c×24

with integer coefficients a, b, c.

## Results
- All 15 particles fit exactly
- Integer coefficients found
- 6 new particles predicted

## Quick Start
```bash
python scripts/verify_simple.py

## **Step 4: Initialize Git repository**

```powershell
# Initialize git
git init

# Add files
git add .

# Commit
git commit -m "Initial commit: A5 particle physics discovery"